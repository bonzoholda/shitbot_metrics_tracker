{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "commands": ["pip install -r requirements.txt"]
        },
        "start": {
          "cmd": "uvicorn tracker:app"
        }
      }
    }
  }
}
